import { Partner } from "src/modules/partners/partner.model";
import { Subscription } from "src/modules/subscriptions/subscription.model";

const entities = [Partner, Subscription];
export default entities; 